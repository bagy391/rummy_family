{
  "name": "family-rummy",
  "version": "1.0.0",
  "private": true,
  "description": "Family Rummy - Multiplayer Progressive Web App",
  "scripts": {
    "dev": "pnpm --filter @rummy/web dev",
    "build": "pnpm --filter @rummy/shared build && pnpm --filter @rummy/web build",
    "test": "pnpm -r test",
    "test:shared": "pnpm --filter @rummy/shared test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@vercel/node": "^5.8.21",
    "agora-token": "^2.0.5"
  }
}
